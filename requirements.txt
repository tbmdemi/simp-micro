# AuxForge - Core dependencies
numpy>=1.24
scipy>=1.10
matplotlib>=3.7

# MMA optimizer (simp/core/mma_runner.py, stiffness_mode/optimizer='mma') -
# xem EXPERIMENT_LOG.md mục 2026-07-30: OC hai-multiplier tự viết thất bại
# (không có cơ chế trust-region), nlopt.LD_MMA (Svanberg 1987) là giải pháp
# đã kiểm chứng cho đúng vấn đề đó (nhiều ràng buộc non-linear tổng quát).
nlopt>=2.7

# ML dependencies - THỰC SỰ dùng bởi pipeline/phase3_dataset (sklearn),
# pipeline/phase4_surrogate + pipeline/phase5_cvae (torch); KHÔNG còn
# optional (trước đây thiếu ở đây khiến `pip install -r requirements.txt`
# không đủ để chạy Phase 3-5 - xem AUDIT_REPORT_INDEPENDENT_2026-07-29.md
# mục D3). Version đã dùng để train checkpoint hiện có (2026-07-29):
# torch 2.12.0+cu130, scikit-learn 1.9.0, pandas 3.0.3.
torch>=2.0
scikit-learn>=1.3
pandas>=2.0
Pillow>=10.0

# Optional: analysis dependencies (report/hình ảnh phân tích, không bắt buộc
# để chạy Phase 0-5)
# scikit-image>=0.20
# seaborn>=0.13

# Phase 8.1: đối chiếu FE độc lập (analysis/scripts/skfem_homogenization.py) -
# implementation FEM khác hoàn toàn engine nội bộ simp/core/, dùng để kiểm
# chứng vật lý (không tái dùng code hiện có) - xem EXPERIMENT_LOG.md 2026-07-31.
scikit-fem>=10.0

# Optional: dev dependencies
# pytest>=7.0
# pytest-cov>=4.0
# flake8>=6.0
# black>=23.0
# mypy>=1.0
